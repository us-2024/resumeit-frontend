import { FC } from 'react'

export const BuilderNavbar: FC<{}> = ({}) => {
    return (
        <div className="fixed z-50 w-full col-span-5 px-5 py-3 bg-slate-100">
            Builder Navbar
        </div>
    )
}
